# yaml-language-server: $schema=https://schema.blue-build.org/module-stage-list-v1.json

modules:
  - type: script
    scripts:
      - common/msttfonts.sh

  - type: script
    scripts:
      - autofirma/install_autofirma.sh
